# Montantbaseht

Montant de la base HT pour cette ligne de TVA.

## Properties

Name | Type | Description | Notes
------------ | ------------- | ------------- | -------------

## Example

```python
from factpulse.models.montantbaseht import Montantbaseht

# TODO update the JSON string below
json = "{}"
# create an instance of Montantbaseht from a JSON string
montantbaseht_instance = Montantbaseht.from_json(json)
# print the JSON string representation of the object
print(Montantbaseht.to_json())

# convert the object into a dict
montantbaseht_dict = montantbaseht_instance.to_dict()
# create an instance of Montantbaseht from a dict
montantbaseht_from_dict = Montantbaseht.from_dict(montantbaseht_dict)
```
[[Back to Model list]](../README.md#documentation-for-models) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to README]](../README.md)


