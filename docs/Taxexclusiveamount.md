# Taxexclusiveamount

Total amount excluding VAT

## Properties

Name | Type | Description | Notes
------------ | ------------- | ------------- | -------------

## Example

```python
from factpulse.models.taxexclusiveamount import Taxexclusiveamount

# TODO update the JSON string below
json = "{}"
# create an instance of Taxexclusiveamount from a JSON string
taxexclusiveamount_instance = Taxexclusiveamount.from_json(json)
# print the JSON string representation of the object
print(Taxexclusiveamount.to_json())

# convert the object into a dict
taxexclusiveamount_dict = taxexclusiveamount_instance.to_dict()
# create an instance of Taxexclusiveamount from a dict
taxexclusiveamount_from_dict = Taxexclusiveamount.from_dict(taxexclusiveamount_dict)
```
[[Back to Model list]](../README.md#documentation-for-models) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to README]](../README.md)


