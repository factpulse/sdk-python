# Taxexclusiveamount1

Amount excluding VAT

## Properties

Name | Type | Description | Notes
------------ | ------------- | ------------- | -------------

## Example

```python
from factpulse.models.taxexclusiveamount1 import Taxexclusiveamount1

# TODO update the JSON string below
json = "{}"
# create an instance of Taxexclusiveamount1 from a JSON string
taxexclusiveamount1_instance = Taxexclusiveamount1.from_json(json)
# print the JSON string representation of the object
print(Taxexclusiveamount1.to_json())

# convert the object into a dict
taxexclusiveamount1_dict = taxexclusiveamount1_instance.to_dict()
# create an instance of Taxexclusiveamount1 from a dict
taxexclusiveamount1_from_dict = Taxexclusiveamount1.from_dict(taxexclusiveamount1_dict)
```
[[Back to Model list]](../README.md#documentation-for-models) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to README]](../README.md)


