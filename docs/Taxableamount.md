# Taxableamount

Taxable amount (base)

## Properties

Name | Type | Description | Notes
------------ | ------------- | ------------- | -------------

## Example

```python
from factpulse.models.taxableamount import Taxableamount

# TODO update the JSON string below
json = "{}"
# create an instance of Taxableamount from a JSON string
taxableamount_instance = Taxableamount.from_json(json)
# print the JSON string representation of the object
print(Taxableamount.to_json())

# convert the object into a dict
taxableamount_dict = taxableamount_instance.to_dict()
# create an instance of Taxableamount from a dict
taxableamount_from_dict = Taxableamount.from_dict(taxableamount_dict)
```
[[Back to Model list]](../README.md#documentation-for-models) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to README]](../README.md)


