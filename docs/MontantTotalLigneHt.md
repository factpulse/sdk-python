# MontantTotalLigneHt

Montant total HT de la ligne (quantité × prix unitaire - remise).

## Properties

Name | Type | Description | Notes
------------ | ------------- | ------------- | -------------

## Example

```python
from factpulse.models.montant_total_ligne_ht import MontantTotalLigneHt

# TODO update the JSON string below
json = "{}"
# create an instance of MontantTotalLigneHt from a JSON string
montant_total_ligne_ht_instance = MontantTotalLigneHt.from_json(json)
# print the JSON string representation of the object
print(MontantTotalLigneHt.to_json())

# convert the object into a dict
montant_total_ligne_ht_dict = montant_total_ligne_ht_instance.to_dict()
# create an instance of MontantTotalLigneHt from a dict
montant_total_ligne_ht_from_dict = MontantTotalLigneHt.from_dict(montant_total_ligne_ht_dict)
```
[[Back to Model list]](../README.md#documentation-for-models) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to README]](../README.md)


