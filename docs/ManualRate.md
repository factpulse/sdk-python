# ManualRate

VAT category rate (BT-119). (Accepte number, string ou integer)

## Properties

Name | Type | Description | Notes
------------ | ------------- | ------------- | -------------

## Example

```python
from factpulse.models.manual_rate import ManualRate

# TODO update the JSON string below
json = "{}"
# create an instance of ManualRate from a JSON string
manual_rate_instance = ManualRate.from_json(json)
# print the JSON string representation of the object
print(ManualRate.to_json())

# convert the object into a dict
manual_rate_dict = manual_rate_instance.to_dict()
# create an instance of ManualRate from a dict
manual_rate_from_dict = ManualRate.from_dict(manual_rate_dict)
```
[[Back to Model list]](../README.md#documentation-for-models) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to README]](../README.md)


